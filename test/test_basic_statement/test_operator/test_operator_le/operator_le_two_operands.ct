# 测试小于等于运算符：两个整数直接比较
Contract OperatorLeTwoOperandsTest:
    def test_le_two(a: int, b: int, c: int, d: int):
        # 比较前两个参数
        # is_le1: bool = a <= b
        # 比较后两个参数
        is_le2: bool = c <= d