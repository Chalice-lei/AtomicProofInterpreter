Contract BranchBothConsume:
    def main(flag: bool, value: string, result: string):
        if flag:
            value = Hash256(value)
            Delete(value)
        else:
            value = Hash256(value)
            Delete(value)
        Return(result)
