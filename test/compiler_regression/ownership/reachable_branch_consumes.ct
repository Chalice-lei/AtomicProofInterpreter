Contract ReachableBranchConsumes:
    def main(flag: bool, value: string):
        if flag:
            Delete(value)
        else:
            marker = Push(1)
            Delete(marker)
        Return(value)
